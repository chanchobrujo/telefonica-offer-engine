server:
  port: 9565

spring:
  application:
    name: APIREST-OFFER-ENGINE
  datasource:
    url: jdbc:mysql://localhost:3306/telefonicabd?useSSL=false&serverTimezone=America/Lima
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
